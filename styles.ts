import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  top: {
    paddingTop: '2.5%',
  },
  topText: {
    fontSize: 50
  },
  choicesContainer: {
    paddingTop: '15%',
  },
  choice:{
    margin: 50,
    fontSize: 100
  }
});